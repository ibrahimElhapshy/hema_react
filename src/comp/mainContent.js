import React from 'react';
import './mainContent.css';

const MainContent = ({pageName}) => {
  return (
    <div>
        <main> {pageName} </main>
    </div>
  );
}

export default MainContent;
